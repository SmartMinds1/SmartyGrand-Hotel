/* const Form = ({ handleSubmit, newUser, setNewUser }) => {
  return (
    <form onSubmit={handleSubmit}>
      <input
        type="text"
        size="20"
        placeholder="Your Name"
        value={newUser}
        onChange={(e) => setNewUser(e.target.value)}
        autoCapitalize="on"
        required
      />

      <button type="submit">Add User</button>
    </form>
  );
};

export default Form;
 */
