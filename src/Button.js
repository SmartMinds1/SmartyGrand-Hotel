import "./Button.css";

//This is the button logic for all buttons
const Button = () => {
  return (
    <button className="btn" type="submit">
      signIn
    </button>
  );
};

export default Button;
